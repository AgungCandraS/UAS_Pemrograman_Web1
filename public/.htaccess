Options -Indexes

<IfModule mod_rewrite.c>
    RewriteEngine On

    # Lokasi aplikasi kamu sebenarnya:
    RewriteBase /bisnisku-web-app/public/

    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^ index.php [L]
</IfModule>
